{
  "name": "godzina-finansisty",
  "version": "1.0.0",
  "description": "Samouczące się narzędzie promptów finansowych dla Claude (PL)",
  "private": true,
  "scripts": {
    "build": "bash scripts/build.sh && node scripts/katalog.cjs",
    "test": "bash scripts/build.sh && NODE_PATH=$(npm root -g) node tests/e2e.cjs && NODE_PATH=$(npm root -g) node tests/offline.cjs",
    "katalog": "node scripts/katalog.cjs",
    "test:offline": "bash scripts/build.sh && NODE_PATH=$(npm root -g) node tests/offline.cjs"
  },
  "license": "MIT"
}